<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
</head>
<body>
  <%- include('../partials/navbar.ejs') %>
  <a href="/category">&lt; Back To Categories List</a>
  <h1><%= title %></h1>
  <a href="/category/<%= category.id %>/update">Update Category</a>
  <table>
    <tr>
      <th>ID</th>
      <th>Name</th>
      <th>Price</th>
      <th>Quantity</th>
      <th></th>
    </tr>
    <% for (let i = 0; i < items.length; i++) { %>
      <tr>
        <td><%= items[i].id %></td>
        <td><%= items[i].name %></td>
        <td><%= items[i].price %></td>
        <td><%= items[i].quantity %></td>
        <td><a href="/item/<%= items[i].id %>">Item Details</a></td>
        <td>
          <form action="/category/<%= category.id %>/item/<%= items[i].id %>/remove" method="POST">
            <button 
              type="submit" 
              onclick="return confirm('Remove <%= items[i].name %> from <%= category.name %>?')"
            >
              Remove
            </button>
          </form>
        </td>
      </tr>
    <% } %>
  </table>
</body>
</html>